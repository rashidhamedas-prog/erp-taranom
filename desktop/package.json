{
  "name": "crm-taranom-desktop",
  "productName": "CRM Taranom",
  "version": "1.0.1",
  "description": "CRM Taranom - Offline Desktop (Windows) with central-server sync",
  "main": "main.js",
  "author": "Taranom",
  "license": "UNLICENSED",
  "scripts": {
    "prepare-server": "node scripts/prepare-server.js",
    "start": "npm run prepare-server && electron .",
    "dist:win": "npm run prepare-server && electron-builder --win nsis --x64",
    "dist:linux": "npm run prepare-server && electron-builder --linux AppImage --x64"
  },
  "dependencies": {
    "express": "^4.18.2",
    "better-sqlite3": "^9.4.3",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "compression": "^1.7.4",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "node-cron": "^3.0.3",
    "sharp": "^0.33.3",
    "nodemailer": "^6.9.13",
    "electron-updater": "^6.3.9"
  },
  "devDependencies": {
    "electron": "^31.3.0",
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "ir.taranom.crm.desktop",
    "productName": "CRM Taranom",
    "asar": false,
    "files": [
      "main.js",
      "server/**/*",
      "!server/node_modules",
      "!server/crm.db*",
      "!server/*.db*",
      "!server/backups/**",
      "node_modules/**/*"
    ],
    "directories": { "output": "dist", "buildResources": "build" },
    "win": {
      "target": [{ "target": "nsis", "arch": ["x64"] }],
      "icon": "build/icon.png"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "shortcutName": "CRM Taranom",
      "installerLanguages": ["en_US"],
      "unicode": true
    },
    "linux": { "target": ["AppImage"], "icon": "build/icon.png", "category": "Office" },
    "publish": [{ "provider": "generic", "url": "https://example.com/releases/" }]
  }
}
